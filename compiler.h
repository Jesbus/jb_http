#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <vector>

using namespace std;

extern string compileConfigScript(string confFile, vector<string>* strings, vector<string>* regexs, vector<string>* inputs);
